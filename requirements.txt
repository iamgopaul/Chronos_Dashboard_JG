# Chronos dashboard — CPU-friendly stack (Railway). Use GPU images locally if needed.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pandas>=2.0.0
numpy>=1.24.0
torch==2.2.2
transformers>=4.36.0
accelerate>=0.26.0
# chronos-forecasting pulls in the `chronos` package used by `app/chronos_service.py`
chronos-forecasting>=2.2.2
